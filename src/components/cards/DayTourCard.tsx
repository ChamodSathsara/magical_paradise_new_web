import React from 'react';
import Link from 'next/link';
import { ArrowRightIcon, CalendarIcon, ClockIcon } from 'lucide-react';
import type { DayTour } from '../../data/dayTours';

export function DayTourCard({tour}:{tour:DayTour}){return <Link href={`/day-tours/${tour.slug}`} className="group flex h-full flex-col overflow-hidden rounded-lg border border-jungle/10 bg-white shadow-card focus:outline-none focus:ring-2 focus:ring-gold"><div className="aspect-[16/10] overflow-hidden bg-sand"><img src={tour.image} alt={`${tour.title} in Sri Lanka`} loading="lazy" className="h-full w-full object-cover transition-transform duration-700 group-hover:scale-[1.06]"/></div><div className="flex flex-1 flex-col p-6"><p className="eyebrow text-gold-dark">{tour.category}</p><h3 className="mt-2 font-serif text-xl text-jungle">{tour.title}</h3><p className="mt-3 line-clamp-3 text-sm leading-6 text-jungle-muted">{tour.shortDescription}</p><div className="mt-5 space-y-2 text-xs text-jungle-muted">{tour.travelDuration?.[0]?<p className="flex gap-2"><ClockIcon className="h-3.5 w-3.5 shrink-0 text-gold-dark"/>{tour.travelDuration[0]}</p>:null}{tour.whenToGo?<p className="flex gap-2"><CalendarIcon className="h-3.5 w-3.5 shrink-0 text-gold-dark"/>{tour.whenToGo}</p>:null}</div><span className="mt-auto inline-flex items-center gap-2 pt-6 text-[11px] font-medium uppercase tracking-[.14em] text-gold-dark group-hover:text-jungle">Explore Tour <ArrowRightIcon className="h-3.5 w-3.5 transition-transform group-hover:translate-x-1"/></span></div></Link>}

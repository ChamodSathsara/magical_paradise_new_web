import Link from 'next/link';
import { Building2Icon, CreditCardIcon, FileCheckIcon, ShieldCheckIcon } from 'lucide-react';
import { PageHero } from '../components/ui/PageHero';
import { Reveal } from '../components/ui/Reveal';
import { IMAGES } from '../data/media';

const OPTIONS=[{icon:Building2Icon,title:'Bank Transfer',text:'Pay securely by bank transfer using the account details provided on your official booking invoice. Clients are responsible for local and international transfer charges.'},{icon:CreditCardIcon,title:'Credit Card',text:'Card payment can be arranged through our approved payment process. A card-processing service fee may be added and will be confirmed before payment.'},{icon:FileCheckIcon,title:'Booking Deposit',text:'A 50% advance payment is generally required to confirm your booking. The amount or schedule may be adjusted by our team on a case-by-case basis.'},{icon:ShieldCheckIcon,title:'Clear Confirmation',text:'Once payment is received, we issue a formal validation note. We do not charge hidden booking or postage fees.'}];
export function PaymentOptions(){return <><PageHero eyebrow="Simple & Secure" title="Payment Options" subtitle="Clear payment arrangements designed to make confirming your journey straightforward." image={IMAGES.hotel} imageAlt="Luxury Sri Lankan hotel interior"/><section className="bg-ivory py-20 lg:py-28"><div className="mx-auto max-w-content px-6"><div className="grid gap-6 sm:grid-cols-2">{OPTIONS.map((o,i)=>{const Icon=o.icon;return <Reveal key={o.title} delay={i*.05}><div className="h-full rounded-lg border border-jungle/10 bg-white p-8 shadow-card"><span className="flex h-12 w-12 items-center justify-center rounded-full bg-jungle text-gold-light"><Icon className="h-5 w-5" strokeWidth={1.5}/></span><h2 className="mt-5 font-serif text-2xl text-jungle">{o.title}</h2><p className="mt-3 text-sm leading-7 text-jungle-muted">{o.text}</p></div></Reveal>})}</div><Reveal className="mx-auto mt-12 max-w-3xl rounded-lg bg-sand p-8 text-center"><h2 className="font-serif text-3xl text-jungle">Need payment assistance?</h2><p className="mt-3 text-sm leading-7 text-jungle-muted">Contact our team before sending funds if you need confirmation of your invoice, payment schedule or applicable fees.</p><Link href="/contact" className="mt-6 inline-flex rounded-full bg-jungle px-7 py-3.5 text-xs font-semibold uppercase tracking-[.16em] text-ivory">Contact Our Team</Link></Reveal></div></section></>}
